# career · 入口规则

本项目根目录的 `CLAUDE.md` 仅作为入口文件使用，具体项目约束已迁移到 `AGENTS.md`。

## 读取顺序

1. 先以 UTF-8 编码读取 `./AGENTS.md`
2. 如果任务涉及创建或修改 UI、布局、样式、交互、动效、文案呈现，再以 UTF-8 编码读取 `./DESIGN.md`
3. 父目录 `E:\职业生涯\CLAUDE.md` 中的通用规则继续生效

## 说明

- 改规则时，先改文档，再改实现
- 不要把项目约束重新写回本文件
